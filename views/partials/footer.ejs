<footer class ="barra-cinzenta">
    <% if(typeof tipo !== 'undefined' && tipo === 'jogo') { %>
        <p class= "dica-jogo"><strong>Dica:</strong>As letras podem estar em sequência direta ou indireta.</p>
        <a href="javascript:history.back()" class="btn-voltar-link">Voltar</a>
    <% } else { %>
        <a href="javascript:history.back()" class="btn-voltar-link">Voltar</a>
    <% } %>
</footer>